# 파이프라인 전용 (서버 사이드) — 실제 값은 .env(로컬)와 GitHub Actions Secrets에만 둔다 (NFR-3)
YOUTUBE_API_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
SUPABASE_ANON_KEY=

# web/ 은 별도로 web/.env.local 사용 (anon key만 — service key 금지)
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
